YAMADA WALKTHROUGH:
The given data is in base 64

Decode it using an online decoder-encoder or just decode it using base64 from the terminal.. Then just grep out your flag which has the format Paradoxctf{}

 DIRECT COMMAND:  cat yamada.txt | base64 -di | grep "Paradoxctf"



 Flag: Paradoxctf{d1cc0v3s3_b3sb0w0R_0f_t1m3_4nd_mu51c}

